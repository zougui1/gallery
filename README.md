# TODO

* home page: page with all images
* search bar in home page, (search by, username/characterName/artistName)
* design of everything! (home/user/upload/image)
* replace the 12th color by the eraser
* add "overlay's helper" modal
* eraseSize range visible only if the eraser is on use
* make the inputs with a button

# bugs
* `NaN` in the pagination when the filter is used
* scaling bug, use the image's max size to draw an overlay (reminder, calcul the draw's position with the scrolltop of the window)
* `username` is by default set as "zougui" in the DB when the images are uploaded

# priorities

## 1
* fix all bugs

## 2
* replace the 12th color by the eraser
* eraseSize range visible only if the eraser is on use
* make the inputs with a button

## 3
* add "overlay's helper" modal

## 4
* home page
* search bar in home page
* re-design everything (complexe/complete)
* replace the pagination with infinite scroll
